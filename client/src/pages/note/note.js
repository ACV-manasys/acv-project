import React, { } from 'react';
import {
  Box,
} from '@mui/material';

import Navbar from '../../components/Navbar';

function Note() {

  return (
    <Box >
      <Navbar active="Note" />
    </Box>
  );
}

export default Note;
